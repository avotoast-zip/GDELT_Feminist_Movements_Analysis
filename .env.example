# Copy to .env and fill in. .env is gitignored and must never be committed.
# Gemini API key, Tier 1 (paid). The free tier silently returns API_ERROR rows
# under load — see docs/known_issues.md.
GOOGLE_API_KEY=your-key-here
